[package]
name = "shaders"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
spirv-std = { version = "0.10.0-alpha.1" }
glam = {
    version = "0.33.3",
    default-features = false,
    features = ["libm", "u32", "i32", "f64"],
}